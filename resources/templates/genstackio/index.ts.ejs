<% locales.forEach(locale => { %>const <%= locale %> = require('<%- dir || '../translations' %>/<%= locale %>.json');
<% }) %>
const translations = {
<% locales.forEach(locale => { %>   '<%= locale.replace('_', '-') %>': <%= locale %>,
<% }) %>
};

export default translations;